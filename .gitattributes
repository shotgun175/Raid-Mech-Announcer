* text=auto

# Keep Rust toolchain files LF so Git for Windows' autocrlf=true doesn't emit
# phantom "LF will be replaced by CRLF" warnings on every status/diff.
# (Both workflows run on windows-latest; this is purely a local-noise fix.)
*.toml text eol=lf
Cargo.lock text eol=lf
