# Actions only: npm and cargo get security updates from the repo settings, not scheduled PRs.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
