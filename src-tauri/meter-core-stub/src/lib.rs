// Local stub — replaces the private meter-core-rs git dependency.
// Only used when building without the "meter-core" feature (log-viewer mode).
// The real crate is never compiled in this configuration.
